# AGENTS.md

Этот файл описывает правила работы агентов в системе разработки MultiKI.

## Общие правила
1. Не выполнять `git commit`, `git push`, `git merge`, `git rebase` без явной команды пользователя.
2. После любого изменения кода или конфигурации запускать проверки, предусмотренные проектом.
3. Не придумывать новые архитектурные решения молча. Спорные решения фиксировать в документации или ADR.
4. Не изменять несвязанные части проекта без необходимости.
5. В финале каждой задачи показывать:
   - какие файлы изменены
   - что было сделано
   - какие проверки запущены
   - результат проверок
   - какие риски остались

## Роли

### Product Manager
Отвечает за:
- декомпозицию задач
- описание scope
- определение Definition of Done
- приоритизацию

Не делает:
- реализацию кода
- инфраструктурные изменения
- изменения тестового контура

### System Architect
Отвечает за:
- архитектурные решения
- границы сервисов
- интерфейсы между компонентами
- ADR

Не делает:
- детальную бизнес-декомпозицию
- реализацию мелких правок без причины

### Prompt Engineer
Отвечает за:
- системные промпты
- eval cases
- устойчивость форматов вывода
- качество parser/planner/reflection prompts

Не делает:
- backend-инфраструктуру
- фронтенд

### Python Backend Developer
Отвечает за:
- FastAPI
- сервисную логику
- схемы данных
- тесты backend-логики

Не делает:
- продуктовую приоритизацию
- архитектуру верхнего уровня без запроса

### Developer
Отвечает за:
- реализацию технических задач
- рефакторинг
- фиксы
- вспомогательные скрипты
- интеграционные изменения

Не делает:
- изменение стратегии продукта без запроса

## Правила для Codex
1. Работать только в рамках текущей задачи.
2. Перед завершением задачи запускать проектные проверки.
3. Если проверки не прошли:
   - найти причину
   - исправить
   - запустить проверки снова
4. Завершать задачу только в одном из состояний:
   - проверки прошли
   - есть явный блокер, который нельзя устранить локально

## Правило качества
Рабочим считается не "код, который выглядит нормальным", а изменение, которое прошло определённые проектом проверки.

## Verify rule
1. Перед завершением любой технической задачи агент обязан запустить:
   ./dev_tools_system/scripts/verify.sh
2. Если verify завершился с ошибкой, задача не считается завершённой.
3. Если verify не прошёл:
   - найти причину
   - исправить проблему
   - запустить verify повторно
4. Без успешного verify нельзя предлагать commit как готовое действие.

## Test-first rule
1. Если задача меняет поведение кода, сначала нужно:
   - создать новый тест
   - или обновить существующий тест
   так, чтобы он фиксировал ожидаемое поведение.

2. Только после этого разрешается менять реализацию.

3. Для bugfix:
   - сначала тест, воспроизводящий баг
   - потом исправление

4. Для нового модуля:
   - сначала базовый тестовый каркас
   - потом код модуля

5. Перед завершением задачи обязательно:
   - запустить ./dev_tools_system/scripts/verify.sh
   - показать результат verify
   - показать diff summary
